{
  "name": "nexium",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "dev": "bun run --cwd web dev",
    "build": "bun run --cwd web build",
    "lint": "bun run --cwd web lint"
  }
}
